#EnginePredictivemaintenance Model

## Steps to run

1. Install dependencies:
pip install -r requirements.txt

2. Run the API:
uvicorn app:app --reload

3. Open browser:
http://126.0.0.1:8000/docs

#Sample Input
use sample_input.jgon for testing predictions
